"use client";

export default function UploadPage() {
    return (
        <main>
            <input type="file" />
            <textarea placeholder="コメント（任意）" />
            <button>投稿</button>
        </main>
    );
}